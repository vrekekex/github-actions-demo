name: CI
on:
  push:
    branches: ['master']
  workflow_dispatch: {}
jobs:
  checkout-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - run: ls -la